# TW-RESEARCH-GITHUB-001 — CI on PRs and pushes to main (editorial canon branch).
name: CI

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  quality:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Citation punctuation regression
        run: npm run verify:citations

      - name: Production build
        run: npm run build
