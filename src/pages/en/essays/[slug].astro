---
import EssayArticlePage from '../../../views/EssayArticlePage.astro';
import { essayDetailStaticPaths } from '../../../lib/i18n/static-paths';

export async function getStaticPaths() {
  return essayDetailStaticPaths('en');
}

const { locale, publicationId, slug } = Astro.props;
---

<EssayArticlePage locale={locale} publicationId={publicationId} slug={slug} />
